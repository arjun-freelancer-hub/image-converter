.mt-6
    label.block.text-sm.font-medium.text-gray-700.mb-2(for="format") Convert to:
    select#format(
        class="w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500"
        name="format"
    )
        each format in supportedFormats
            option(value=format.toLowerCase())= format 